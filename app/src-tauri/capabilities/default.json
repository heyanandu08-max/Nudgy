{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main (settings) window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
