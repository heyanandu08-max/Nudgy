{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window and the per-monitor overlays",
  "windows": [
    "main",
    "overlay-*"
  ],
  "permissions": [
    "core:default"
  ]
}
